#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: Returns the result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
