#include "main.h"

/**
 * add - Adds two integers
 * @x: One of the integers to be added
 * @y: The second integer
 *
 * Return: Returns the result
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
