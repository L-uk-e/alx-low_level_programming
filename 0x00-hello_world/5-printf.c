#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Uses printf to output some random text
 *
 * Return: Always returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
